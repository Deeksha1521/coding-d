#include<stdio.h>
int main()
{
 int n,i;
 printf("enter the numbers:");
 scanf("%d",&n);
 printf("odd numbers are:");
 for(i=1;i<=n;i++)
 {
     if(i%2!=0)
     {
          printf("%d",i);
     }
 }
 return 0;
 }
