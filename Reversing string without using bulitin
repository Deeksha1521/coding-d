#include<stdio.h>
#include<string.h>
int main()
{
    int i,n;
    char str[100];
    printf("Enter string to be reversed");
    gets(str);
    n=strlen(str);
    printf("reversed string is\n");
    for(i=n-1;i>=0;i--)
    {
        printf("%c",str[i]);

    }

    }
